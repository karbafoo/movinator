const UserController = require('./user');
const MovieController = require('./movie');
const ListController = require('./movie/list.controller');

module.exports = {
    UserController,
    MovieController,
    ListController,
};
